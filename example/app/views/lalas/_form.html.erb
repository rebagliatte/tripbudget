<%= form_for(@lala) do |f| %>
  <% if @lala.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lala.errors.count, "error") %> prohibited this lala from being saved:</h2>

      <ul>
      <% @lala.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
